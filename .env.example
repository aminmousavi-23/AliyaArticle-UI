# Leave unset to use the Vite dev proxy (see vite.config.ts), which forwards
# /api requests to http://localhost:5000 — the server URL in v1.json.
#
# Set this if your backend runs somewhere else, or for production builds
# where there's no dev proxy:
# VITE_API_BASE_URL=http://localhost:5000
